{
  "name": "zenith-printhub-api",
  "version": "1.0.0",
  "description": "Mock API using json-server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "0.17.4"
  },
  "engines": {
    "node": "18.x"
  }
}